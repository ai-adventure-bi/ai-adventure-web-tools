import http from 'node:http';
import {readFile} from 'node:fs/promises';
import {extname, join, normalize} from 'node:path';
import {fileURLToPath} from 'node:url';

const ROOT=fileURLToPath(new URL('.',import.meta.url));
const PORT=Number(process.env.PORT||4173);
const SERVER_API_KEY=(process.env.HEYOTTO_API_KEY||'').replace(/^Bearer\s+/i,'').replace(/[^\x21-\x7E]/g,'');
const API_URL='https://api.chat.heyotto.app/v1/chat/completions';
const MAX_BODY=64*1024;

const TOP_EXAMPLE={toyType:'top',complexity:65,designSeed:4837,bodyTheme:'coral',themeAmount:6,themeSize:5,bodyDiameter:60,bodyHeight:20,stemHeight:25,stemDiameter:9,tipLength:8,rimStyle:'wavy',rimPattern:'radial',rimSweep:2,armSpiral:3,armReach:4,bodyCrownStyle:'jewelled',bodyCrownSize:3,bodyCrownHeight:2,bodyCrownLobes:8,bodyCrownTwist:1,handleStyle:'spire',pommelStyle:'simple',hubStyle:'halo',hubHeight:3,hubDepth:2,hubLobes:8,hubTwist:0,lobes:7,waveDepth:3,rimLift:1,secondaryLobes:12,twist:2,handleTwist:2,handleFins:2,handleFlare:3,holes:0,holeSize:3,topperStyle:'dolphinTail',topperHeight:24,topperWidth:18,topperFeatures:6,topperTwist:1,topperRelief:3,color:'#39bfc4'};
const YOYO_EXAMPLE={toyType:'yoyo',complexity:65,diameter:58,width:34,profileStyle:'vortex',profileCurve:6,grooveStyle:'rounded',grooveWidth:5,grooveDepth:10,hubDiameter:11,hubStyle:'flower',hubRelief:2,rimStyle:'ripple',rimLobes:10,rimDepth:3,rimTwist:5,faceDish:3,faceStyle:'spiral',facePetals:8,faceRelief:3,faceTwist:7,swirlDepth:4,color:'#56b7e8'};
const BOOTSTRAP=`You are the design interpreter for TopBot, a child-friendly tool that makes functional, 3D-printable spinning tops and solid yo-yos. Translate each child's request into exactly one complete JSON recipe. Output JSON only: no markdown, explanation, preamble or code fence. Preserve current values unless the child asks to change them. Never add unknown fields. Choose imaginative combinations while retaining a centred tip, sensible handle and printable geometry. Use a six-digit hexadecimal color.

SPINNING TOP: every key in this example is required:
${JSON.stringify(TOP_EXAMPLE)}
Allowed named values: bodyTheme none|coral|scales|bubbles|armour|craters|mushrooms; rimStyle smooth|wavy|scalloped|spiky|organic|petal|enchanted|crystal; rimPattern radial|turbine|hooked|comet|claw; bodyCrownStyle none|jewelled|tiara|petals|crystal|runes; handleStyle straight|flared|mushroom|twisted|crown|spire|chalice|vine|winged; pommelStyle simple|orb|crystal|flame|crown; hubStyle smooth|crown|petal|gear|crystal|halo; topperStyle is a handle-integrated character and must be none|dolphinTail|tentacle|trident|coral|skull|cyclops|mushroom|robot|ghost|crystal.
Numeric ranges: complexity 0-100; designSeed 0-9999; themeAmount 0-10; themeSize 0-10; bodyDiameter 25-65; bodyHeight 8-30; stemHeight 8-34; stemDiameter 5-12; tipLength 3-12; rimSweep -10-10; armSpiral -12-12; armReach 0-10; bodyCrownSize 0-10; bodyCrownHeight 0-8; bodyCrownLobes 3-18; bodyCrownTwist -10-10; hubHeight 0-12; hubDepth 0-8; hubLobes 3-16; hubTwist -10-10; lobes 0-16; waveDepth 0-10; rimLift 0-10; secondaryLobes 0-24; twist 0-10; handleTwist 0-10; handleFins 0-12; handleFlare 0-10; holes 0-20; holeSize 2-7; topperHeight 14-34; topperWidth 10-25; topperFeatures 3-12; topperTwist -10-10; topperRelief 0-7. designSeed and all counts must be whole numbers. Prefer rounded child-safe silhouettes; never interpret spiky as needle-sharp.
Choose a fresh designSeed when the child requests a substantially new concept so pupils can receive distinct arrangements. Aim for controlled weirdness: use one strong silhouette idea plus one or two quieter details. Ordinary fantasy designs can use waveDepth 3-7, rimLift 1-5 and armReach 3-9; avoid setting every ornament to maximum. Requests for ruffles, fabric, skirts, flowers, ballerinas or intensely wavy shapes should be visibly dramatic: prefer rimStyle wavy, petal or enchanted; lobes 8-14; waveDepth 7-10; rimLift 5-9; armReach 6-10; and bodyHeight 14-22. This deliberate ruffled-body exception may be highly theatrical, but keep the tip and handle centred and sturdy. A named topperStyle replaces the upper part of the handle instead of adding height above it: stemHeight controls the total grip-plus-totem height, while topperHeight controls how much of that total is occupied by the ornamental totem. Use topperHeight 20-30 for a prominent, clearly defined ornament and 14-19 for a smaller accent. Sculptural, strongly defined profiles are welcome; use stemHeight 12-22, stemDiameter 7-12 and pommelStyle simple. For an underwater coral request, use bodyTheme coral with themeAmount 5-8 and themeSize 3-7; choose topperStyle dolphinTail when the child asks for a dolphin or whale tail. For dolphinTail specifically use stemHeight 14-18, stemDiameter 8-12, handleStyle flared, chalice or spire, pommelStyle simple and handleFins 0-2 so its broad notched flukes dominate the silhouette.

YO-YO: every key in this example is required:
${JSON.stringify(YOYO_EXAMPLE)}
Allowed named values: profileStyle classic|saucer|bell|flower|vortex|relic; grooveStyle rounded|deep|wide|vortex; rimStyle smooth|wavy|scalloped|petal|dragon|ripple|cloud|vortex; faceStyle smooth|sunburst|runes|scales|spiral|vortex|petals|orbit; hubStyle round|flower|star|gem|orbit.
Numeric ranges: complexity 0-100; diameter 40-70; width 20-44; profileCurve 0-10; grooveWidth 3-9; grooveDepth 4-14; hubDiameter 8-18; hubRelief 0-5; rimLobes 0-18; rimDepth 0-7; rimTwist -10-10; faceDish 0-7; facePetals 0-18; faceRelief 0-5; faceTwist -10-10; swirlDepth 0-6. rimLobes and facePetals must be whole numbers. The remaining radius after grooveDepth must exceed the hub radius by at least 1.5 mm. Prefer smooth flowing combinations rather than sharp teeth. Use profileStyle vortex or saucer with faceStyle spiral or vortex and swirlDepth 3-6 for strongly swirled designs. Flower, petal and cloud values create softer organic toys; relic, orbit and gem values create ancient mechanical designs. Preserve a continuous central string trench and balanced matching halves.

The latest user message contains CURRENT_RECIPE and CHILD_REQUEST. Return the same toyType as CURRENT_RECIPE unless the child explicitly requests the other toy family.`;

function json(res,status,data){const body=JSON.stringify(data);res.writeHead(status,{'content-type':'application/json; charset=utf-8','content-length':Buffer.byteLength(body),'cache-control':'no-store'});res.end(body);}
async function readJson(req){let size=0,chunks=[];for await(const chunk of req){size+=chunk.length;if(size>MAX_BODY)throw new Error('Request is too large.');chunks.push(chunk);}return JSON.parse(Buffer.concat(chunks).toString('utf8'));}
function connectionError(error){const code=error?.cause?.code||error?.code;if(code==='ENOTFOUND')return 'TopBot could not find api.chat.heyotto.app. Check the internet connection or DNS settings.';if(code==='ECONNREFUSED')return 'HeyOtto refused the connection. Its API may be temporarily unavailable.';if(code==='CERT_HAS_EXPIRED'||code==='UNABLE_TO_VERIFY_LEAF_SIGNATURE'||code==='SELF_SIGNED_CERT_IN_CHAIN')return `A secure certificate check failed (${code}). A school network filter may be intercepting HTTPS.`;if(error?.name==='TimeoutError'||error?.name==='AbortError')return 'HeyOtto did not respond within 45 seconds.';return `TopBot could not reach HeyOtto${code?` (${code})`:''}. ${error?.cause?.message||error?.message||'Connection failed.'}`;}
function containsJsonObject(content){if(typeof content!=='string')return false;const first=content.indexOf('{'),last=content.lastIndexOf('}');if(first<0||last<=first)return false;try{const value=JSON.parse(content.slice(first,last+1));return value&&typeof value==='object'&&!Array.isArray(value);}catch{return false;}}
async function callHeyOtto(payload,apiKey){let response;try{response=await fetch(API_URL,{method:'POST',headers:{authorization:`Bearer ${apiKey}`,'content-type':'application/json'},body:JSON.stringify(payload),signal:AbortSignal.timeout(45000)});}catch(error){return {networkError:connectionError(error)};}const text=await response.text();let data;try{data=JSON.parse(text);}catch{data={error:{message:`HeyOtto returned an unreadable ${response.status} response.`}};}return {response,data};}
export async function chat(req,res){const headerKey=Array.isArray(req.headers['x-heyotto-api-key'])?req.headers['x-heyotto-api-key'][0]:req.headers['x-heyotto-api-key'];const apiKey=String(headerKey||SERVER_API_KEY||'').replace(/^Bearer\s+/i,'').replace(/[^\x21-\x7E]/g,'');if(!apiKey)return json(res,401,{error:'Enter your HeyOtto API key to use AI design.'});if(!/^ak_[A-Za-z0-9._~-]{8,}$/.test(apiKey))return json(res,401,{error:'The supplied HeyOtto API key is not valid.'});try{const input=await readJson(req);if(!Array.isArray(input.messages)||!input.messages.length||input.messages.length>30)return json(res,400,{error:'A valid conversation is required.'});if(input.messages.some(message=>!message||!['user','assistant'].includes(message.role)||typeof message.content!=='string'||message.content.length>2000))return json(res,400,{error:'The conversation contains an invalid message.'});if(!input.current_recipe||typeof input.current_recipe!=='object'||Array.isArray(input.current_recipe))return json(res,400,{error:'The current design recipe is required.'});const firstMessage=`${BOOTSTRAP}\n\nCURRENT_RECIPE for this request:\n${JSON.stringify(input.current_recipe)}`;const messages=[{role:'user',content:firstMessage},...input.messages.map(message=>({role:message.role,content:message.content}))];let payload={model:'heyotto',messages,stream:false};if(typeof input.conversation_id==='string'&&/^[A-Za-z0-9._~-]{1,200}$/.test(input.conversation_id))payload.conversation_id=input.conversation_id;let result=await callHeyOtto(payload,apiKey);if(result.networkError)return json(res,502,{error:result.networkError});if(!result.response.ok)return json(res,result.response.status,{error:result.data?.error?.message||`HeyOtto could not complete that request (${result.response.status}).`});let content=result.data?.choices?.[0]?.message?.content;if(!containsJsonObject(content)){const correction='Your previous response was not usable by TopBot. Reply again with exactly one complete JSON object matching the required recipe. Do not greet, explain, apologise, use markdown, or ask a question.';payload={...payload,messages:[...messages,{role:'assistant',content:String(content||'')},{role:'user',content:correction}],conversation_id:result.data.conversation_id||payload.conversation_id};result=await callHeyOtto(payload,apiKey);if(result.networkError)return json(res,502,{error:result.networkError});if(!result.response.ok)return json(res,result.response.status,{error:result.data?.error?.message||`HeyOtto could not correct its response (${result.response.status}).`});content=result.data?.choices?.[0]?.message?.content;}if(!containsJsonObject(content)){const excerpt=String(content||'empty response').replace(/\s+/g,' ').slice(0,180);return json(res,422,{error:`Otto replied without a usable JSON recipe: “${excerpt}”`});}return json(res,200,{content,conversation_id:result.data.conversation_id,usage:result.data.usage});}catch(error){return json(res,400,{error:error.message||'The request could not be completed.'});}}

const MIME={'.html':'text/html; charset=utf-8','.json':'application/json; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.png':'image/png','.svg':'image/svg+xml'};
async function serve(req,res){const pathname=new URL(req.url,'http://localhost').pathname;const relative=pathname==='/'?'index.html':decodeURIComponent(pathname.slice(1));const target=normalize(join(ROOT,relative));if(!target.startsWith(ROOT))return json(res,403,{error:'Forbidden'});try{const body=await readFile(target);res.writeHead(200,{'content-type':MIME[extname(target)]||'application/octet-stream','cache-control':'no-store'});res.end(body);}catch{json(res,404,{error:'Not found'});}}

if(process.argv[1]&&fileURLToPath(import.meta.url)===normalize(process.argv[1]))http.createServer((req,res)=>{if(req.method==='POST'&&req.url==='/api/design')return chat(req,res);if(req.method==='GET'||req.method==='HEAD')return serve(req,res);json(res,405,{error:'Method not allowed'});}).listen(PORT,'127.0.0.1',()=>console.log(`TopBot is ready at http://127.0.0.1:${PORT}`));
