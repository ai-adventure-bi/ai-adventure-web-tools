{
  "name": "ai-adventure-web-tools",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "npm --prefix quake-quest ci && npm --prefix quake-quest run build && node scripts/assemble-vercel-site.mjs"
  }
}
